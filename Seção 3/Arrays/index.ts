let numbers:number[] = [1, 2, 3];
numbers.push(1);

console.log(numbers[2]);

// numbers = ""; //Não permitido, pois o tipo é diferente

const nomes:string[] = ["Marcos", "Vinicius"];

nomes.push("Angeli");

/**
 * Sintaxe alternativa para arrays (Mais antiga)...
 */

const nums: Array<number> = [1, 2, 3];

nums.push(4);
