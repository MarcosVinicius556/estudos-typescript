/**
 * Basicamente, um tipo annotation é quando definimos manualmente o
 * tipo de uma variável (assim -> ":<tipo>")
 */
const annotation: string = "Este é por annotation";

/**
 * E o tipo inferencia é quando deixamos o ts decidir qual será
 * o tipo da variável
 */

const inference = "Este é por inferência";